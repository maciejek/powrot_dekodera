
<h1>Uploadowanie plików</h1>

<table>
  <tr>   
    <th>Sciezka</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pliki.each do |plik| %>
<% mojString = plik.sciezka %>
  <tr>    
    <td><%= mojString %></td>
    <td><%= link_to 'Show', lista_plikow_path(plik), :method=>:get %></td>
    <td><%= link_to 'Edit', edit_lista_plikow_path(plik) %></td>
    <td><%= link_to 'Destroy', lista_plikow_path(plik), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'dodaj nowy plik', new_lista_plikow_path %>
